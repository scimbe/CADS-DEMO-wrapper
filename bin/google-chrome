#!/bin/sh
# fresh isolated profile so headless --print-to-pdf doesn't attach to the running GUI Chrome
exec "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --user-data-dir="$(mktemp -d /tmp/cchrome.XXXXXX)" --no-first-run --no-default-browser-check "$@"
